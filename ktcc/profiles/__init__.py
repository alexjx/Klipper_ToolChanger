"""Per-tool print, motion, and thermal profiles.

Import from ``models`` or ``service`` explicitly; package import has no side
effects and therefore cannot create a ports/service initialization cycle.
"""
